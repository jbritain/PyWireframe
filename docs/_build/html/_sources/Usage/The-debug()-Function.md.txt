# The `debug()` Function

## What does it do?
The `debug()` function is designed for debug purposes. It prints the value of various variable. The variables it prints are

```python
CameraX
CameraY
CameraZ
FocalLength
objects
```

Note that `objects` is actually a list.